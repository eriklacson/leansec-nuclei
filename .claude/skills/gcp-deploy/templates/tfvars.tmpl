# GENERATED BY gcp-deploy skill from ./deployment.yaml
# Do not edit this file directly — re-running the skill regenerates it.
# Source: deployment.yaml fields → infra/gcp/variables.tf

# ─── Required ───
project_id    = "{{gcp_project_id}}"
client_name   = "{{client_name}}"
targets_file  = "{{targets_file}}"
profiles_file = "{{profiles_file}}"

# ─── Scanner image ───
scanner_image = "{{scanner_image}}"

# ─── Region ───
region = "{{region}}"

# ─── Schedule ───
schedule_cron     = "{{schedule_cron}}"
schedule_timezone = "{{schedule_timezone}}"

# ─── Feature flags ───
enable_scheduler = {{enable_scheduler}}
enable_wif       = {{enable_wif}}
enable_ar_mirror = {{enable_ar_mirror}}

# ─── WIF (only used when enable_wif = true) ───
wif_github_repository = "{{wif_github_repository}}"


# ─── Resource sizing — defaults from module; uncomment to override ───
# results_retention_days = 365
# results_delete_days    = 730
# job_memory             = "2Gi"
# job_cpu                = "1"
# job_timeout            = "3600s"
