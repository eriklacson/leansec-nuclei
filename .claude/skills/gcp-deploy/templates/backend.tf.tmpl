# GENERATED BY gcp-deploy skill from ./deployment.yaml
# Do not edit this file directly — re-running the skill regenerates it.
# State bucket created by scripts/bootstrap-gcp-client.sh.

terraform {
  backend "gcs" {
    bucket = "{{gcp_project_id}}-tfstate-leansecurity-nuclei"
    prefix = "nuclei-scanner"
  }
}
