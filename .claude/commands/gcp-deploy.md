Deploy the leansecurity-nuclei pipeline to GCP using the gcp-deploy skill.

Load and execute the skill at `.claude/skills/gcp-deploy/SKILL.md` against the current working directory as the target deployment folder. Follow all workflow steps in SKILL.md exactly, starting from step 1 (detect deployment folder state).
