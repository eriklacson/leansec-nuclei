# AWS Infrastructure Module

Not yet implemented. Follow the same variable interface as `infra/gcp/`.

## Required Resources

- S3 buckets (config + results)
- ECS Fargate task definition
- EventBridge rule (monthly schedule)
- IAM roles (task execution + scheduler)
