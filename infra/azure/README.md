# Azure Infrastructure Module

Not yet implemented. Follow the same variable interface as `infra/gcp/`.

## Required Resources

- Azure Blob Storage containers (config + results)
- Azure Container Apps job
- Azure Logic App (monthly schedule)
- Managed identities (container + scheduler)
